# Wave 2 — `app-engines` stream — PHASE A plan (Area C: C1, C4, C5, C6)

Branch `gaps/app-engines`, worktree `/home/j/experiments/daemon-worktrees/app-engines`, based on
daemon-app `master` @ `fea462f`. **Plan only — no implementation.** Vertical owner of the foreign-agent
(`Agent*`) facade + agent-registry UI. Sibling wave-2 streams: `app-approvals-safety`
(ApprovalCard/inbox internals, D3 structured prompts, fingerprint/tool_list/ApprovalDecide-reason),
`app-delegation` (Caps/notice/UnitNode fields), `app-channels-liveness` (TransportChanged/FleetChanged).

---

## 0. Code-reality survey — what exists vs. what the stories expect

### Wire (v29 codec already regenerated in master @ `f92107a`)
The vendored generated codec already carries the full v29 surface — **no `just update-codec` needed**:
- `agent_entry { name, recipe, source, protocol?, installed?, version?, capabilities? }`
  (`daemon_api_client_types.h:1633`).
- `agent_recipe { program?, args[64], env[64 kv], endpoint? }` (`:1584`); `agent_source =
  Builtin|Manual|Endpoint` (`:1595`); `agent_protocol = Acp|StreamJson` (`:1603`).
- `request_agent_register { AgentRegister_entry: agent_entry }` (`:1647`), `request_agent_remove` (`:1651`),
  `api_request_request_agent_discover_m_c` / `…_catalog_m_c` / `…_register_m_c` / `…_remove_m_c` (`:3084`).
- `engine_selector = Core | engine_foreign{ Foreign{ agent } }` (`:773`) — wire renamed `Acp`→`Foreign`.
- `response_agent_catalog` (`:5226`); `unit_node_engine` (`:3866`) — **owned by `app-delegation`, not me.**

### App facade (still mechanically `Acp`-named; wave-1 seams to build on, not duplicate)
- **`Chip` control** (`Controls/Chip.qml`, wave-1 `6afc54a`): label + glyph + tone + `interactive`/
  `closable` + `tooltipText`. Reuse verbatim for every chip below.
- **Engine-identity facade + chip (C3/ENG-7 LANDED, `4e684e4`)**: `DecodedProfileSpec.engineKind`
  (`"Core"|"Acp"`) + `engineAcpAgent`; `DaemonProfileStore::profile(id)` returns a QVariantMap with
  `engine`/`acpAgent` (`daemon_profile_store.cpp:377`); session→engine join in QML
  (`ComposerControls.sessionEngineInfo()` via `SessionSettings.profileFor` + `Profiles.profile`); fleet-row
  join in `DaemonFleetTree` (profileRef→profile). Engine chips render on **profile rows** (`ProfilesPage.qml:100`),
  **fleet rows** (`FleetPage.qml:185`), **session header** (`ComposerControls.qml:79`), and TUI
  (`composer_chrome.cpp:159`, `hub_profiles.cpp:13` `engineToken`). All compare `=== "Acp"`.
- **Shared engine picker (ENG-2/CON-16 PARTIAL, `47108df`)**: `AgentTypePicker.qml` (GUI) +
  `AgentTypeView`/`agent_type_view.{h,cpp}` (TUI), both driven by `AcpRepository` (`AcpAgents` context
  property). Renders native row 0 + catalog rows (name/source/installed/version); `refresh()` issues
  `refreshCatalog()` **and** `discover()`. Mounted by `NewAgentDialog.qml` (GUI) and `tui_dialogs.cpp`
  NewAgentDialog + the wizard agent-type step.
- **`AcpRepository`** (`repositories.{h,cpp}:731` / `:2180`): **read-only** — `refreshCatalog()`
  (`encodeAcpCatalogRequest`) + `discover()` (`encodeAcpDiscoverRequest`), `agents()` → `[{name, source,
  installed, version}]`, `catalogRefreshed()`/`operationFailed()`. **No register/remove. No `protocol`.**
- **Codec facade**: `DecodedAcpAgentEntry { name, source, installed, version }` — **no `protocol`**
  (`node_api_codec.h:137`); `encodeAcpCatalogRequest`/`encodeAcpDiscoverRequest` only (no register/remove);
  `decodeAcpCatalog` (`decode_responses.cpp:1091`) reads the renamed `agent_entry` generated struct but
  drops `recipe` + `protocol` + `capabilities`; `mappers.cpp:541` maps `engine_foreign`→`engineKind="Acp"`.
- **Foreign affordance (C4) — only checkpoints done**: `ComposerControls.foreignSession` (`=== "Acp"`,
  `:42`) gates only `CheckpointsPopover.foreignSession` (Restore hidden + "Rewind is managed by the
  foreign agent" copy, `CheckpointsPopover.qml:56`). **ModelPill, model chip, session-settings overrides,
  profile-editor inference are NOT yet foreign-aware.**
- **Approvals (C5 insertion targets)**: `ApprovalsPage.qml` (inbox) and `ToolApprovalBar.qml` (in-transcript
  bar). Rows built by `DaemonApprovalsInbox::rebuild()` (`daemon_approvals_inbox.cpp:31`) carrying
  `{id, session, tool, command, risk, requested, canAllowPermanent}` — **no engine/origin.** `session` IS
  present → engine derivable via the wave-1 session→profile→engine join. **These files are the
  `app-approvals-safety` sibling's D3 territory.**
- **Failure surfacing (C6) reality today**:
  - create-time: `createAcpProfile` (`daemon_profile_store.cpp:232`) returns an optimistic id immediately;
    `NewAgentDialog` closes + `openNewAgentChat()`. A `ProfileCreate` rejection (unknown/uninstalled agent,
    proven node-side) arrives **async** as `ProfileRepository::operationFailed`→`profileOpFailed` — a raw
    toast after the dialog already closed. No field-level error.
  - session-open: `SessionCreate` error ("agent `<name>` is not installed") flows through the normal
    session/turn error path (`ApiResponseKind::Error`).
  - mid-turn death: `TurnFinished{ end_reason: Failed }` surfaces via the existing turn error state
    (`turn_controller.h:26` state `"error"`; `daemon_turn_engine.cpp:590` "The turn ended: %1"). Generic;
    no foreign grammar; **no node conformance test for mid-turn death** (per ENG-8 Reveals).
- **Cache schema** `kSchemaVersion = 6` (`client_cache_schema.h:22`). The agent catalog is live-refreshed
  in-memory, not persisted as cache rows → **no bump anticipated** (see Risks).

### Gap summary against the stories
| Item | Story | State today | This wave |
|---|---|---|---|
| C1 | ENG-3/ENG-4 | read-only `AcpRepository`; no register/remove/protocol | build register+remove facade + settings surface (GUI+TUI) |
| C4 | ENG-3/ENG-4 | only checkpoints hide for foreign | extend `foreignSession` honesty to model/tool/override surfaces |
| C5 | ENG-6 | approval rows carry no origin | origin engine chip via wave-1 facade join; minimal tagged insert |
| C6 | ENG-8 | generic errors; async raw toast; no mid-turn grammar | actionable register/create/session/mid-turn states within node signals |

---

## 1. Facade-rename proposal — **RECOMMEND RENAME NOW** (`Acp*` → `Agent*`, `engineKind "Acp"` → `"Foreign"`)

**Recommendation: rename the app facade to match the wire, and change the `engineKind` sentinel
`"Acp"`→`"Foreign"`, carrying ACP-vs-stream-json as a separate `protocol` field.** Rationale:

1. **Correctness, not cosmetics.** v29 made `Acp` a *protocol* (`agent_protocol = Acp|StreamJson`), not
   the engine kind — the wire engine selector is `Foreign{ agent }`. Keeping `engineKind == "Acp"` now
   actively lies for a stream-json foreign engine (ENG-5). The chip label "…(ACP)" must come from the
   catalog entry's `protocol`, not from the engine kind.
2. **I own this vertical.** ENG-5 is the largest rename surface and it is entirely engine-identity code
   (C3 chips + the picker + the repo) — my domain. Doing it piecemeal later means a second pass over the
   same files.
3. **Fallout is bounded and mechanical** (full list below). No behavior change beyond the label source.

**Fallout list (all become `// [wave2:app-engines]`-tagged edits):**
- `node_api_codec.h`: `DecodedAcpAgentEntry`→`DecodedAgentEntry` (+ new `protocol`, `capabilities?` fields);
  `engineKind` default/comment `"Acp"`→`"Foreign"`; method decls `encodeAcpCatalogRequest`→
  `encodeAgentCatalogRequest`, `encodeAcpDiscoverRequest`→`encodeAgentDiscoverRequest`, `decodeAcpCatalog`→
  `decodeAgentCatalog`; `ApiResponseKind::AcpCatalog`→`AgentCatalog`.
- `node_api_codec/mappers.cpp:541,1030` : `engineKind = "Foreign"`; foreign-agent get/set arms.
- `node_api_codec/decode_responses.cpp:75,1091` : rename + decode `protocol`.
- `node_api_codec/encode_requests.cpp:312,989` : rename.
- `repositories.{h,cpp}`: `AcpRepository`→`AgentRepository`, correlations `repo/acp-*`→`repo/agent-*`.
- `app_service_graph.{h,cpp}`: `graph.acp`→`graph.agents`; QML context property `AcpAgents`→`Agents`.
- `iprofile_store.h` + `daemon_profile_store.{h,cpp}`: `createAcpProfile`→`createForeignProfile`;
  `engineKind = "Foreign"`; row `engine` value `"Foreign"`.
- QML engine-chip sites: `ComposerControls.qml`, `ProfilesPage.qml`, `FleetPage.qml` — `=== "Acp"` →
  `=== "Foreign"`; label suffix from `protocol`. `AgentTypePicker.qml` (`AcpAgents`→`Agents`,
  `acpRows`→`agentRows`), `NewAgentDialog.qml` (`createAcpProfile`→`createForeignProfile`).
- TUI: `composer_chrome.cpp:159`, `hub_profiles.cpp:13` (`== "Acp"`→`"Foreign"`, protocol-aware label),
  `agent_type_view.{h,cpp}` (`AcpRepository`→`AgentRepository`), `tui_dialogs.{h,cpp}`,
  `root_widget_input.cpp` (`m_services.acp`).
- Tests: `tst_daemon_fleet_tree.cpp`, `tst_first_run_model.cpp` reference engine strings; update.

**Migration nuance (call out to coordinator):** the profile's wire engine selector carries only
`Foreign{ agent }` — **not** the protocol. To render "…(ACP)" vs "…(stream-json)" on a profile/session/
fleet chip requires a second join (agent name → catalog `AgentRepository` entry → `protocol`). The chip's
primary signal stays the **agent name**; the protocol suffix is best-effort when the catalog is loaded and
degrades to a bare "Foreign"/agent name otherwise. I'll expose a facade helper `protocolForAgent(name)`.

**If the coordinator vetoes the rename:** fallback is keep `AcpRepository`/`engineKind "Acp"` names but
STILL add the `protocol` field + register/remove methods under the old names, and gate the "(ACP)" label
on `protocol == "Acp"`. This leaves the codebase saying "Acp" where the wire says "Foreign" — I advise
against it, but it is a clean, smaller diff.

---

## 2. C1 — foreign-agent register / remove (management surface)

**Facade (C++, mine).** Extend the repo (renamed `AgentRepository`) with the two write ops + protocol:
- `DecodedAgentEntry`: add `QString protocol = "Acp"` (`"Acp"|"StreamJson"`); decode it in
  `decodeAgentCatalog`.
- Codec:
  - `encodeAgentRegisterRequest(const DecodedAgentRecipeInput& in)` — builds `request_agent_register` →
    `agent_entry { name, protocol, source=Manual, recipe{ program|endpoint, args, env } }`. `installed`/
    `version`/`capabilities` are left absent (the node **re-probes and never trusts a caller-supplied
    `installed`** — ENG-3). New small input struct `DecodedAgentRecipeInput { name, protocol, program,
    args(QStringList), env(QVariantMap), endpoint }` in `node_api_codec.h`.
  - `encodeAgentRemoveRequest(const QString& name)` — `request_agent_remove{ name }`.
  - Response handling: register/remove reply is a bare `Ok`/`Error` (reuse `responseKind`/`decodeError`).
- `AgentRepository`: `Q_INVOKABLE void registerAgent(const QVariantMap& form)` (form = the input struct
  flattened for QML), `Q_INVOKABLE void removeAgent(const QString& name)`; on Ok, chain a
  `refreshCatalog()` + `discover()` so the list reflects the node's re-probe; signals `agentRegistered()`,
  `agentRemoved(name)`, existing `operationFailed(msg)`. Correlations `repo/agent-register`,
  `repo/agent-remove`.

**Shared view-model.** A thin `AgentCatalogController` (new `src/core/daemon/agent_catalog_controller.{h,cpp}`,
or fold onto `AgentRepository` as Q_INVOKABLEs — prefer folding to avoid a new class) exposing: `rows()`
(name/source/protocol/installed/version), `register(form)`, `remove(name)`, plus `manualNames()` for the
remove affordance. Both GUI and TUI bind to this **one** object (the existing `Agents` context property).

**GUI.** New Settings section **"Agents"** (id `agents`) in `SettingsPage.qml` (append to `sections` +
`Loader` switch + a `Component`). New `Pages/AgentsSettingsSection.qml`:
- Header + "Re-scan installed" (`Agents.discover()`) + "Register custom…" button.
- Catalog `ListView`: each row = name, source badge (`Kit.Chip` tone muted/accent), protocol chip
  ("ACP"/"stream-json"), installed badge (reuse the picker's badge pattern), version. Manual rows get a
  `Kit.IconButton`/`closable` chip → Remove.
- Register form as a `Kit.Dialog` (`Pages/RegisterAgentDialog.qml`): Name (`Kit.TextField`), Protocol
  (`Kit.` segmented/`ComboBox` ACP vs stream-json), a stdio/endpoint toggle → Program + Args (repeatable
  rows) + Env (kv rows) **or** Endpoint (`tcp://…`), a probe-result panel (C6), Save→`Agents.register(form)`.
- Remove confirm (`Kit.Dialog`) stating the consequence: "profiles referencing `<name>` fail closed at
  next spawn" (ENG-3 acceptance) before `Agents.remove(name)`.

**TUI.** Parity via the shared controller. Add an "Agents" subsection to the Settings hub page
(`hub_settings.cpp`) OR a dedicated managed page — recommend a section in `hub_settings` listing rows with
`[installed]`/`[manual]`/`[acp|stream-json]` markers, a key to open a **register overlay** dialog
(`tui_dialogs`: reuse the `NewAgentDialog` form idiom — name, protocol list, program/args/env or endpoint,
inline probe result) and a key to remove the highlighted manual row (with a confirm). Wire in
`root_widget_input.cpp` (tagged) + `root_widget_wiring.cpp`.

---

## 3. C4 — foreign affordances (surfaces adapt honestly)

Driver: the existing `foreignSession`/`engine == "Foreign"` facade signal. Survey of what must adapt beyond
checkpoints (already done):

| Surface | Native | Foreign treatment | File |
|---|---|---|---|
| Checkpoints/rewind | timeline + Restore | **done** (Restore hidden + copy) | `CheckpointsPopover.qml` |
| Model picker (`ModelPill`) | provider models | **hide/disable** — foreign engines resolve no provider/model; tooltip "Model is chosen by the foreign agent" | `ComposerControls.qml:181` |
| Session-settings popover | effort/model/mode overrides | **hide model + effort overrides** for foreign (keep approval-policy mode — approvals DO apply to foreign, ENG-6); honest note | `SessionSettingsPopover.qml` |
| Profile editor (GUI dialog / TUI `profile_editor_dialog`) | provider/model/key/persona | **hide inference config** for a `Foreign` profile; show "runs a foreign engine — recipe managed by the daemon" (mirror `NewAgentDialog`) | GUI profile editor, `tui/dialogs/profile_editor_dialog.cpp` |
| Quantization | n/a (model-level, not per session) | no change | — |
| Approval policy chip | shown | **keep** (foreign requests obey session policy — ENG-6) | `ComposerControls.qml:93` |

- Keep the wave-1 pattern: **hide with an explanatory tooltip/line, never silently omit** (C4 honesty).
- Chip label: foreign session header chip shows agent name + protocol suffix (from `protocolForAgent`).
- TUI parity: `composer_chrome.cpp` already renders the engine chip; add the model-token suppression for
  foreign in the composer chrome and the inference suppression in `profile_editor_dialog.cpp`.

Shared-model note: `foreignSession` is currently computed in QML. To keep TUI parity clean for the new
suppressions, expose the session→foreign check as a facade helper (`engineForSession(sessionId)` returning
`{engine, agent, protocol}`) — reused by C5 too (§4). This is a small shared C++ helper (see §4), not
logic in QML.

---

## 4. C5 — approval origin attribution (minimal, clearly-bounded)

**Design.** The approval row already carries `session`; engine identity is a property of the session's
profile (session→profile→engine), the exact wave-1 join. No node work needed — this is honest attribution,
not fabrication.

**Component I own.** `Controls/EngineOriginChip.qml` (a thin wrapper over `Kit.Chip`): property
`sessionId`; internally resolves `{engine, agent, protocol}` via a **shared C++ facade helper**
`EngineIdentity.engineForSession(sessionId)` (new `src/core/daemon/engine_identity.{h,cpp}`, exposed as
context property `EngineIdentity`, wrapping the profile store + session-settings pointers already in the
graph). Renders nothing for native sessions (no noise), and `⬡ <agent> · ACP`/`· stream-json` for foreign.
The C++ helper is reused by ComposerControls' `sessionEngineInfo()` (refactor to call it — removes the QML
duplication) and by the TUI approval overlay for parity.

**Insertion (the overlap).** The card/inbox internals are the **`app-approvals-safety` sibling's D3
territory**. To avoid collision:
- I deliver the reusable `EngineOriginChip.qml` + the `EngineIdentity` facade in files I own.
- The insertion into `ApprovalsPage.qml` (inbox row, near `entry.session`, line ~83) and
  `ToolApprovalBar.qml` (in-transcript bar header) is a **single `// [wave2:app-engines]`-tagged line**:
  `EngineOriginChip { sessionId: entry.session }` (and the callId/session equivalent in the bar).
- **Coordinator sequencing ask:** land `app-approvals-safety`'s D3 card rewrite FIRST, then apply my
  one-line insert onto the final card markup; OR the sibling accepts my component and adds the single line.
  Either way the component + facade are self-contained and mergeable independently.

**TUI.** The approval overlay/row (in the approvals hub page / `interactive_turn_host` park view) renders
a bracketed `[<agent> · ACP]` origin token via `EngineIdentity.engineForSession` — same helper, parity kept.

---

## 5. C6 — foreign failure states (within what the node reports today)

Survey → design, staged earliest-first (ENG-8: create > session-open > mid-turn > probe):

1. **Registration probe (ENG-3/ENG-8).** `AgentRegister` re-probes; the node's reply is `Ok` (entry
   persisted with probed `installed`/`version`) or `Error`. The register dialog's probe panel renders:
   - success → "handshake ✓ · ACP `<version>`" (from the refreshed catalog entry) or, for stream-json,
     "found on PATH" (PATH-only probe, no version — per the v29 plan §2f).
   - failure → surface the node's `Error` message via `operationFailed`, distinguishing **"binary not
     found"** from **"handshake failed/timeout"** *iff the node's error string distinguishes them*
     (see Risks — may be a deferred node-first ask if the string is opaque).
2. **Create-time (ENG-8, proven node-side).** Make `createForeignProfile` failures **field-level** instead
   of a post-close toast: `NewAgentDialog`/`AgentTypePicker` keep open and show the agent-named error.
   Requires routing `profileOpFailed` back into the dialog's accept gate (don't `openNewAgentChat()` until
   the create is acknowledged). This is a small state change in `NewAgentDialog.qml` +
   `DaemonProfileStore` (surface a per-op result/last-error). TUI mirrors in `tui_dialogs`.
3. **Session-open (agent uninstalled after creation).** `SessionCreate` → `Error("agent <name> is not
   installed")` already flows to the session/turn error path. Add a **session-level error banner** in the
   transcript/session surface with an **"Open Agents settings"** action (deep-link `Nav → settings#agents`,
   the C1 surface) so the user can re-scan/fix. Reuse the existing turn error state; add foreign grammar.
4. **Mid-turn death.** `TurnFinished{ end_reason: Failed }` already ends the turn with a failure summary
   (no silent hang) + keeps streamed content; retry re-spawns. Improve the copy for foreign sessions to
   name the agent + stage where the node message allows; keep the transcript.

**Banner grammar (vision `resolve | spawn | handshake | mid-turn`).** The node's error strings are
free-text today; the client can name the **agent** (from the profile) and echo the node message honestly,
but a *structured* stage code is not on the wire. **Deferred node-first follow-up (record, don't fake):**
a structured foreign-failure enum (`{stage, agent}`) on the error/TurnFinished for reliable stage badging,
plus a conformance test for mid-turn death (ENG-8 Reveals: none exists). Until then C6 renders the honest
node message + agent name + the jump-to-catalog action.

---

## 6. File-touch list (shared-hotspot tags called out)

**Shared hotspots — append-only, `// [wave2:app-engines]`-tagged:**
- `src/core/daemon/node_api_codec.h` — `DecodedAgentEntry` (+`protocol`), `DecodedAgentRecipeInput`,
  `encodeAgentRegisterRequest`/`encodeAgentRemoveRequest` decls, rename block.
- `src/core/daemon/node_api_codec/encode_requests.cpp` — register/remove encoders (+ rename).
- `src/core/daemon/node_api_codec/decode_responses.cpp` — decode `protocol` (+ rename).
- `src/core/daemon/node_api_codec/mappers.cpp` — `engineKind "Foreign"` (rename only).
- `src/core/daemon/repositories.{h,cpp}` — `AgentRepository` register/remove methods + signals.
- `src/core/daemon/app_service_graph.cpp` — construct/expose `Agents`, `EngineIdentity` context props;
  wire register/remove; catalog refresh on ready (rename `graph.acp`).
- `src/tui/root_widget_input.cpp` — bind register/remove/re-scan keys (+ rename `m_services.acp`).
- `src/tui/root_widget_wiring.cpp` — wire the agents settings section/view.
- `src/core/daemon/CMakeLists.txt`, `src/DaemonApp/Pages/CMakeLists.txt`,
  `src/DaemonApp/Controls/CMakeLists.txt`, `src/tui/CMakeLists.txt`, `tests/**/CMakeLists.txt` — register
  new files (append-only lists).
- `src/core/daemon/client_cache_schema.h` — **only if** caching the catalog/protocol proves necessary
  (not anticipated; see Risks). Union at integration if bumped.

**Files I own outright (new):**
- `src/core/daemon/engine_identity.{h,cpp}` (session/profile→engine helper; C4+C5).
- `src/DaemonApp/Controls/EngineOriginChip.qml` (C5).
- `src/DaemonApp/Pages/AgentsSettingsSection.qml`, `Pages/RegisterAgentDialog.qml` (C1).
- `src/tui/dialogs/register_agent_dialog.{h,cpp}` (C1 TUI) + agents section in `tui/pages/hub_settings.cpp`.
- Tests (below).

**Files I edit (mine/vertical, engine-identity + foreign affordances):**
- `iprofile_store.h`, `daemon_profile_store.{h,cpp}` (rename + create-error surfacing).
- QML: `NewAgentDialog.qml`, `AgentTypePicker.qml`, `ComposerControls.qml`, `ProfilesPage.qml`,
  `FleetPage.qml`, `CheckpointsPopover.qml` (already), `SessionsPage`/`Session.qml` (session error banner),
  `SessionSettingsPopover.qml`, `SettingsPage.qml`.
- TUI: `composer_chrome.cpp`, `hub_profiles.cpp`, `agent_type_view.{h,cpp}`, `tui_dialogs.{h,cpp}`,
  `dialogs/profile_editor_dialog.cpp`.

**Overlap files (single tagged insert, coordinate with `app-approvals-safety`):**
- `src/DaemonApp/Pages/ApprovalsPage.qml`, `src/DaemonApp/BlockEditor/qml/components/ToolApprovalBar.qml`
  — one `EngineOriginChip { sessionId: … }` line each (C5).
- TUI approval overlay (approvals hub view / `interactive_turn_host`) — one origin-token line (C5).

**Estimated touch count:** ~10 new files, ~22 edited (of which ~9 shared-hotspot tagged, ~3 overlap
single-line inserts).

---

## 7. Test plan (unit / QML / ctest)

- **Unit (`tests/unit`)**:
  - `tst_agent_registry.cpp` (new): `encodeAgentRegisterRequest` round-trips program/args/env/endpoint +
    protocol → decode back through the generated encoder/decoder; `encodeAgentRemoveRequest`;
    `decodeAgentCatalog` now populates `protocol`; register/remove drive the right correlation and chain a
    catalog refresh; `operationFailed` on `Error`.
  - `tst_engine_identity.cpp` (new): `engineForSession`/`engineForProfile` join returns
    `{engine, agent, protocol}` for native (empty) vs foreign; `protocolForAgent` catalog lookup.
  - extend `tst_daemon_fleet_tree.cpp`, `tst_first_run_model.cpp` for the `"Acp"`→`"Foreign"` rename.
  - create-error path: `createForeignProfile` failure surfaces a field-level error (state on the store).
- **QML (`tests/qml`)**: `tst_agents_settings.qml` (catalog rows render, register form gates on
  name+recipe, remove confirm), `tst_engine_origin_chip.qml` (native→hidden, foreign→agent+protocol),
  `tst_foreign_affordances.qml` (model picker hidden, checkpoints copy) using a mock `EngineIdentity`/`Agents`.
- **TUI (`tests/tui`)**: agents section lists rows + register/remove overlay; foreign profile editor hides
  inference; approval overlay shows the origin token. Respect `tui_parity_tests.cpp` (GUI+TUI parity).
- **Gates**: build (`-DDAEMON_APP_TUI=ON`), clang-format, clang-tidy, `all_qmllint`, ctest incl.
  `i18n_audit`/`i18n_translations`, and `scripts/i18n-drift.sh` **after** the string list is folded in at
  integration (see §8).

---

## 8. New `qsTr` strings — policy note (do NOT touch `i18n/*.ts` this stream)

Per instructions I will **not** run `update_translations` or edit any `i18n/daemon-app_*.ts` catalog in
this stream (concurrent streams would collide on all 12 catalogs). I keep a **running list** for the
coordinator to fold + translate once at integration (then run `scripts/i18n-drift.sh`). Anticipated new
source strings (final wording may shift):
- C1: "Agents", "Foreign agents", "Register custom agent…", "Re-scan installed", "Name", "Protocol",
  "ACP", "stream-json", "Program", "Arguments", "Environment", "Endpoint (tcp://…)", "Stdio", "Register",
  "Remove", "Remove agent?", "Profiles that use “%1” will fail to start until it is re-registered.",
  "This agent runs a foreign engine — its launch recipe is managed by the daemon.",
  "installed"/"not installed" (reuse existing where possible).
- C4: "Model is chosen by the foreign agent", "Not applicable to foreign engines".
- C5: "Requested by %1", "%1 · ACP", "%1 · stream-json" (chip/tooltip).
- C6: "Handshake failed", "Binary not found", "Probing…", "Agent “%1” is not installed",
  "Open Agents settings", "The foreign agent “%1” could not start.", "The turn failed: %1".

---

## 9. Risks / open questions for the coordinator

1. **Facade rename (§1) — approve?** Recommend YES (`Acp*`→`Agent*`, `engineKind "Acp"`→`"Foreign"`,
   protocol split). Touches wave-1 engine-chip QML/TUI (my vertical) + shared codec. Fallback documented.
2. **C5 / approvals-card overlap (§4).** `ApprovalsPage.qml` + `ToolApprovalBar.qml` are the
   `app-approvals-safety` sibling's D3 files. Need a sequencing decision: sibling lands the D3 card first
   and I add the one-line `EngineOriginChip` insert, or the sibling absorbs my component. My component +
   `EngineIdentity` facade are self-contained regardless.
3. **`EngineIdentity` shared helper.** I plan to refactor `ComposerControls.sessionEngineInfo()` (wave-1
   QML) onto a shared C++ helper so C4/C5 and the TUI share one join. This edits a wave-1 file — flag as a
   small, tagged change; acceptable?
4. **C6 stage grammar is node-blocked.** The `resolve|spawn|handshake|mid-turn` badge and reliable
   "binary-not-found vs handshake-failed" distinction need structured error codes the node does not emit
   today; mid-turn death also has no node conformance test (ENG-8 Reveals). I will render the honest node
   message + agent name + jump-to-catalog now and **record a deferred node-first follow-up** for the
   structured `{stage, agent}` error enum. Confirm this is the right call vs. attempting brittle
   string-parsing.
5. **Cache schema.** I do not expect to bump `client_cache_schema.h` (v6) — the agent catalog is
   live-refreshed. If C1 needs to persist `protocol`/manual rows for offline-first parity with other
   repos, I will bump and flag for union at integration. Preference?
6. **C1 home.** GUI: a new **Settings → Agents** section (proposed). Alternative: hang it off the
   `AgentTypePicker`'s "register custom…" row (ENG-3 vision) — but that's more C2 (not my item). I lean
   Settings section as the durable management home + optionally a "register custom…" affordance later.
   Confirm placement.
7. **`app-delegation` owns `unit_node_engine`** (F3). The fleet-row engine chip (C3, wave-1) already reads
   `entry.engine` from the profileRef join; if `app-delegation` starts populating `engine` from the new
   `UnitNode` field, we must not double-source. Flag for integration reconciliation.

---

# PHASE B — LANDING RECORD (app-engines)

**Status: IMPLEMENTED.** All four items (C1, C4, C5, C6) + the approved facade rename landed as
commits on `gaps/app-engines`. Branch based at `master` `fea462f`. No pushes. Worktree only.

Commits (oldest → newest):
- `cfbc00b` refactor(engines): rename `Acp*`→`Agent*` facade, `engineKind "Acp"`→`"Foreign"`
- `a6dfc60` feat(engines): C1 foreign-agent register/remove UI (GUI + TUI)
- `f24c050` feat(engines): C4 foreign-session affordances adapt honestly
- `9b51ee3` feat(engines): C5 approval origin engine chip component
- `35ad620` feat(engines): C6 foreign-agent failure states
- `794790f` test(engines): agent-registry codec, EngineIdentity, EngineOriginChip
- `5991886` fix(engines): clear clang-tidy + i18n-audit findings

## Gate results (run via `nix develop --command …` in the worktree)

| Gate | Command | Exit |
|---|---|---|
| Fresh configure | `cmake -B build -G Ninja -DBUILD_TESTING=ON -DDAEMON_APP_TUI=ON` | 0 |
| Full build | `cmake --build build` | 0 |
| clang-format | `git ls-files 'src/*.cpp' 'src/*.h' 'tests/*.cpp' 'tests/*.h' \| xargs clang-format --dry-run --Werror` | 0 |
| clang-tidy | touched `src/*.cpp` (`git diff --name-only master...HEAD`) `\| xargs -n1 clang-tidy -p build --quiet` | 0 (clean) |
| qmllint | `cmake --build build --target all_qmllint` | 0 |
| ctest (full) | `QT_QPA_PLATFORM=offscreen ctest --test-dir build --output-on-failure` | 0 — **99/99** incl. `i18n_audit` + `i18n_translations` |

(`rm -rf build` for a from-scratch dir is impossible in this worktree — the fetched microtex
resources are read-only Nix-store files; a clean re-`cmake` in place regenerates all build files,
which is the equivalent fresh configure.)

## Per-item outcome

- **C1 — register/remove UI.** `AgentRepository` (renamed) grew `registerAgent(form)` /
  `removeAgent(name)` over `AgentRegister`/`AgentRemove` (new codec encoders
  `encodeAgentRegisterRequest`/`encodeAgentRemoveRequest`; the node re-probes, a caller `installed`
  is never sent, recipes never travel back out). GUI: **Settings → Agents** section
  (`AgentsSettingsSection.qml`) — catalog list (name / source / protocol / installed / version),
  "Register custom…" (`RegisterAgentDialog.qml`: name, protocol, stdio program+args+env OR tcp://
  endpoint), "Re-scan installed", and Remove-with-consequence confirm on manual rows. TUI:
  `AgentsDialog` (`src/tui/dialogs/agents_dialog.{h,cpp}`) — list + register form + remove — opened
  with `g` on the Settings page. Both bind the SAME repository. Register/remove outcomes (incl. the
  node's honest failure cause) surface in the section banner / TUI status line (feeds C6).
- **C4 — foreign affordances.** Driven by the engine-identity facade. Model picker hidden for a
  foreign session (engine chip tooltip says why); session-settings popover hides reasoning-effort +
  fast/verbose (keeps profile override + approval mode — those apply) with an honest note; GUI
  `ProfileEditor` + TUI `profile_editor_dialog` hide provider/model/base-URL for a foreign profile
  and show a read-only engine line. (Checkpoints already read-only for foreign — wave 1.)
- **C5 — approval origin.** Shipped the component + facade ONLY (no card inserts, per decision #2):
  `EngineOriginChip.qml` (Controls) resolves the requesting engine via the shared `EngineIdentity`
  facade and renders a chip naming the foreign agent (nothing for native). `EngineIdentity`
  (`src/core/daemon/engine_identity.{h,cpp}`) is the single session/profile→engine label mapping;
  `ComposerControls.sessionEngineInfo()` and the TUI `composer_chrome` now consume it (parity).
  **Insertion snippets for integration 2 are below.**
- **C6 — failure states.** Register/create/session failures render the node's honest, agent-named
  message: create-time (a rejected foreign `ProfileCreate` re-surfaces the New-agent dialog with the
  error instead of a silent close); session-open / mid-turn (a foreign session in an error turn
  shows a banner in `TranscriptPage` naming the agent + echoing the node message + "Open Agents
  settings"); registration probe (the Agents section / TUI status). The structured stage grammar
  (`resolve|spawn|handshake|mid-turn`) is a **deferred node-first follow-up** (decision #3) — no
  client-side faking. See "Node-first follow-ups" below.

## Facade rename — complete list (decision #1: APPROVED, done)

Wire alignment: the app facade now matches v29 (`EngineSelector::Foreign{agent}`), with the agent's
ACP-vs-stream-json **protocol** carried as a separate catalog-entry field.

- Codec (`node_api_codec.h` + `node_api_codec/*.cpp`):
  `DecodedAcpAgentEntry`→`DecodedAgentEntry` (+ new `protocol` field), new
  `DecodedAgentRecipeInput`; `DecodedProfileSpec.engineAcpAgent`→`engineForeignAgent`;
  `engineKind` default/value `"Acp"`→`"Foreign"`; `encodeAcpCatalogRequest`→`encodeAgentCatalogRequest`,
  `encodeAcpDiscoverRequest`→`encodeAgentDiscoverRequest`, `decodeAcpCatalog`→`decodeAgentCatalog`;
  new `encodeAgentRegisterRequest` / `encodeAgentRemoveRequest`; `ApiResponseKind::AcpCatalog`→`AgentCatalog`;
  mappers decode/encode of `engine_foreign` now set `"Foreign"` + `engineForeignAgent`, decode `protocol`.
- Repository (`repositories.{h,cpp}`): `AcpRepository`→`AgentRepository` (+ `registerAgent`,
  `removeAgent`, `agentRegistered`, `agentRemoved`; correlations `repo/acp-*`→`repo/agent-*`).
- Service graph (`app_service_graph.{h,cpp}`): field `acp`→`agents`; new field `engineIdentity`;
  forward decls; construction of both in `createAppServiceGraph` (both modes).
- App (`DaemonApp/App/application.cpp`): context property `AcpAgents`→`Agents`; new `EngineIdentity`.
- Profiles (`iprofile_store.h`, `daemon_profile_store.{h,cpp}`): `createAcpProfile`→`createForeignProfile`;
  `engineKind = "Foreign"`. **The presentation ROW KEY `acpAgent` was deliberately KEPT** (it is the
  contract read by app-delegation's fleet mapper, which this stream must not touch — boundary #5);
  only the engine VALUE moved to `"Foreign"`.
- First-run (`first_run_model.{h,cpp}`): `applyAcpChoice`→`applyForeignChoice`,
  `applyReflectedAcpChoice`→`applyReflectedForeignChoice`; local `acpAgent`→`foreignAgent`.
- QML: `AgentTypePicker.qml` (`AcpAgents`→`Agents`, `acpRows`→`agentRows`, local `acp`→`agent`,
  protocol-aware label), `NewAgentDialog.qml` (`createForeignProfile`), `ProfilesPage.qml`
  (`=== "Foreign"`), `FirstRunGate.qml` (`applyForeignChoice`), `ComposerControls.qml` (`=== "Foreign"`).
- TUI: `agent_type_view.{h,cpp}` (`AgentRepository`, `DecodedAgentEntry`, `m_acp`→`m_repo`,
  protocol label), `tui_dialogs.{h,cpp}` (`AgentRepository`, `createForeignProfile`),
  `dialogs/first_run_dialog.{h,cpp}` (`AgentRepository`, `applyForeignChoice`),
  `dialogs/profile_editor_dialog.{cpp,h}` (`== "Foreign"`), `pages/hub_profiles.cpp` (engineToken
  `== "Foreign"`), `root_widget.cpp` / `root_widget_input.cpp` (`m_services.acp`→`m_services.agents`),
  `root_widget_pages.cpp` (`== "Foreign"`), `composer_chrome.{h,cpp}` (now via `EngineIdentity`).
- Tests: `tst_first_run_model.cpp` (`ForeignCapableProfileStore`, `createForeignProfile`,
  `applyForeignChoice`), `tst_auth_flow.cpp` (`encodeAgentDiscoverRequest`).
- **NOT touched (boundary #5, app-delegation):** `daemon_fleet_tree.{h,cpp}` and `FleetPage.qml`.
  Their engine block still compares `=== "Acp"` and reads `p["acpAgent"]`. Since the profile store
  now emits engine `"Foreign"`, the FleetPage engine chip renders "Native" for a foreign unit **until
  app-delegation rebinds it to the wire `unit_node.engine`** — an accepted, documented integration
  gap (see risks). The fleet MAPPER read of `p["acpAgent"]` still works (row key kept). Its unit test
  (`tst_daemon_fleet_tree.cpp`) passes engine strings through and is untouched/green.

## C5 insertion snippets for integration 2 (apply onto the merged, D3-redesigned card)

The component + facade are landed and self-contained. `EngineOriginChip` renders nothing for native
sessions (no-noise), so it is safe to insert unconditionally. `sessionId` is the approval row's
session (present on every pending row as `entry.session`).

**GUI — `ApprovalsPage.qml`** (inbox row) and **`BlockEditor/qml/components/ToolApprovalBar.qml`**
(in-transcript bar). Add the import if not already present, then the one line in the row header:
```qml
import DaemonApp.Controls as Kit   // already imported in both files today

// ApprovalsPage.qml — inside the row's header RowLayout (near `entry.session`):
Kit.EngineOriginChip { sessionId: entry.session }   // [wave2:app-engines] C5

// ToolApprovalBar.qml — the bar has no sessionId prop today; the D3 card should thread the row's
// session down (toolData.sessionId or the block's session) and pass it:
Kit.EngineOriginChip { sessionId: root.sessionId }  // [wave2:app-engines] C5
```

**TUI — approval overlay** (the app-approvals-safety D3 park/inbox view). Use the shared facade for
the identical label (parity), via the service graph's `engineIdentity`:
```cpp
// where the parked approval row is built, with `sessionId` in scope and m_services.engineIdentity:
const QVariantMap origin = m_services.engineIdentity->engineForSession(sessionId);
if (origin.value(QStringLiteral("engine")).toString() == QStringLiteral("Foreign")) {
    // append origin.value("label").toString()  e.g. "gemini · ACP"  to the row/overlay
}
```

## Deviations from the Phase A plan

1. **Row key `acpAgent` NOT renamed to `foreignAgent`.** Plan §1 implied renaming it; boundary #5
   (app-delegation owns the fleet mapper that reads this key) forced keeping the presentation key so
   the mapper is not touched. Only the engine VALUE moved to `"Foreign"`. Documented above.
2. **TUI register form env field is single-line** (`KEY=VAL,KEY=VAL`) instead of the GUI's
   multi-line editor — a stated TUI degradation (ZTextEdit needs live terminal text-metrics that are
   fragile at construction; env is niche for the common program+args case; the GUI covers the full
   form). Program/args/endpoint/protocol have full parity.
3. **C5 not inserted** into `ApprovalsPage`/`ToolApprovalBar`/TUI overlay (decision #2) — component +
   facade + snippets only.
4. **C6 create-time** uses a re-surface-on-failure pattern (the async node rejection re-opens the
   New-agent dialog with the error) rather than blocking the modal, because `Kit.Dialog` closes on
   accept. The install-gate already blocks the common uninstalled case client-side.
5. **AgentRepository register/remove has no dedicated unit test** (it is a thin forward to the codec
   + correlation); the wire contract is covered by `tst_agent_registry` and the join by
   `tst_engine_identity`.

## Tests added

- `tests/unit/tst_agent_registry.cpp` — AgentRegister/Remove/Catalog/Discover codec round-trips
  (source=Manual, protocol tag, stdio + endpoint recipes, installed ABSENT) + AgentCatalog `protocol`
  decode. Links `daemon_client` + `daemon_codec`.
- `tests/unit/tst_engine_identity.cpp` — `EngineIdentity` join (native vs foreign, session→profile,
  default fallback) + `labelFor` presentation mapping (ACP / stream-json / unknown / native).
- `tests/qml/engine_chip/tst_engine_origin_chip.qml` (+ `_main.cpp`) — the C5 chip's no-noise
  contract (renders nothing without the facade; component loads clean).

## No `i18n/*.ts` edits — running qsTr list for integration (fold + translate all 12 catalogs)

Per the wave rule I did NOT run `update_translations` or edit any catalog. New user-facing source
strings added this stream (final wording as committed), for integration to extract + translate:

- **C1 (Agents section / RegisterAgentDialog, GUI):** "Foreign agents"; "Agents that run a foreign
  engine (ACP or the Claude Code stream-json bridge). Launch recipes are stored and validated by the
  daemon — profiles reference an agent by name only."; "Register custom…"; "Re-scan installed";
  "Agent registered."; "Removed “%1”."; "No foreign agents in the catalog yet."; "manual"; "builtin";
  "installed"; "not installed"; "Remove"; "Remove agent"; "Remove “%1”? Profiles that use it will
  fail to start until it is re-registered."; "ACP"; "stream-json"; "Register foreign agent";
  "Register"; "Name"; "catalog name (e.g. my-claude)"; "Protocol"; "ACP (Agent Client Protocol)";
  "Claude Code (stream-json)"; "Connect to a TCP endpoint instead of launching a program";
  "Program"; "executable (resolved on the daemon's PATH)"; "Arguments (space-separated)";
  "--flag value"; "Environment (KEY=VALUE per line)"; "Endpoint"; "tcp://host:port"; "The daemon
  probes the recipe on Register (an ACP initialize handshake, or a PATH check for stream-json) — the
  result appears in the agent list."
- **C1 (TUI AgentsDialog):** "Foreign agents"; "Registered + discovered agents:"; "Register…";
  "Remove"; "Re-scan"; "Close"; "Agent registered."; "Removed “%1”."; "(no foreign agents in the
  catalog)"; "Only manually registered agents can be removed."; "Remove agent"; "Remove “%1”?
  Profiles that use it will fail to start\nuntil it is re-registered."; "Cancel"; "Register foreign
  agent"; "Name:"; "Protocol (Enter to pick):"; "ACP (Agent Client Protocol)"; "Claude Code
  (stream-json)"; "Program (blank = use TCP endpoint below):"; "Arguments (space-separated):";
  "Environment (KEY=VAL,KEY=VAL):"; "OR endpoint (tcp://host:port):"; "  [manual]"; "  [builtin]";
  "  [installed]"; "  [not installed]".
- **Rename fallout (reworded existing strings — these reset to unfinished in every catalog):**
  AgentTypePicker "  ·  stream-json" (new) alongside "  ·  ACP %1"; NewAgentDialog "This agent runs a
  foreign engine. Its launch recipe is managed by the daemon's agent catalog — no provider, model, or
  key to configure."; TUI composer_chrome / hub_profiles / profile_editor foreign labels
  ("Engine: foreign agent · %1 (set at create time)").
- **C4:** "Foreign engine — model, reasoning and rewind are managed by the agent"; "Reasoning effort
  and modes are managed by the foreign agent."; ProfileEditor "This agent runs a foreign engine — its
  launch recipe and model are managed by the daemon's agent catalog, so there is no provider, model,
  or base URL to configure here."; "Foreign engine (set at create time)".
- **C5:** "%1 · ACP"; "%1 · stream-json" (EngineIdentity.labelFor); "Foreign"; "Requested by this
  foreign agent" (EngineOriginChip).
- **C6:** "The foreign agent “%1” could not run."; "Open Agents settings"; NewAgentDialog error
  banner (renders the node message verbatim — no new literal). SettingsPage section label "Agents".

## Integration risks / notes for the coordinator

1. **FleetPage engine chip is stale until app-delegation lands (accepted, boundary #5).** The engine
   VALUE is now `"Foreign"` but `FleetPage.qml` + `daemon_fleet_tree.cpp` still compare `"Acp"`
   (untouched, delegation's F3 rebind target). A foreign unit renders "Native" on the Fleet page in
   this branch alone. app-delegation's rebind to `unit_node.engine` (wire value `"Foreign"`) fixes it;
   they should reuse `EngineIdentity.labelFor(engine, agent, protocol)` for the label so all four
   surfaces read identically. **No double-sourcing:** once delegation sources engine from
   `unit_node.engine`, the profileRef join in the mapper can go.
2. **C5 card insertion is deferred to integration 2** (snippets above). The `ToolApprovalBar` needs
   the row's session threaded down (it has no `sessionId` today) — flag for the D3 card owner.
3. **Shared-file edits are tagged `// [wave2:app-engines]`** and append-only in
   `node_api_codec.{h,cpp}`, `repositories.{h,cpp}`, `app_service_graph.{cpp,h}`,
   `root_widget_input.cpp`, and the CMakeLists — integration unions them cleanly.
4. **No `client_cache_schema.h` bump** (stayed v6) — the agent catalog is live-refreshed, not cached.
5. **Node-first follow-ups (backlog):** (a) a structured foreign-failure enum `{stage, agent}` on the
   error / `TurnFinished` so C6 can badge `resolve|spawn|handshake|mid-turn` reliably (today the
   client echoes the node's free-text message + names the agent); (b) a conformance test for foreign
   mid-turn death (ENG-8 Reveals: none exists).
