# core/ holds the plain-C++ libraries (no QML). Added as its libraries come
# online; nothing is wired yet in the minimal foundation.
# add_subdirectory(core)

# QML feature modules (and the app executable).
add_subdirectory(DaemonApp)
