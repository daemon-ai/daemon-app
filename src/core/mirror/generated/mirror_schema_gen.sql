-- @generated by mirror-codegen (daemon-node/crates/contracts/daemon-api/mirror-codegen/entity_codegen.py)
-- Source of truth: daemon-api.cddl + src/core/mirror/entity-map.toml. DO NOT EDIT.
-- Regenerate via `just update-codec` (superproject); guarded by `just codec-drift`.

-- Mirror cache schema (mirror-<id>.db, disposable). Derived from the entity map
-- per spec §4.5. Class-L entities are in-memory only and intentionally have no table.

CREATE TABLE mirror_meta(k TEXT PRIMARY KEY, v TEXT);  -- schema_version, journal_head

CREATE TABLE m_adapters(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  family TEXT NOT NULL,
  display_name TEXT,
  directory INTEGER,
  cap_rooms INTEGER,
  cap_direct_messages INTEGER,
  cap_file_transfer INTEGER,
  ops_json TEXT,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_agent_entries(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  name TEXT NOT NULL,
  source TEXT,
  protocol TEXT,
  installed INTEGER,
  version TEXT,
  verification TEXT,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_command_specs(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  name TEXT NOT NULL,
  summary TEXT,
  category TEXT,
  args_hint TEXT,
  scope TEXT,
  side_effecting INTEGER,
  confirm INTEGER,
  min_access TEXT,
  source TEXT,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_contacts(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  transport TEXT NOT NULL,
  id TEXT NOT NULL,
  display_name TEXT,
  permission TEXT,
  presence_primitive TEXT,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_conversations(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  transport TEXT NOT NULL,
  id TEXT NOT NULL,
  kind TEXT,
  title TEXT,
  topic TEXT,
  description TEXT,
  parent TEXT,
  member_count INTEGER,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_conversation_members(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  transport TEXT NOT NULL,
  conv TEXT NOT NULL,
  contact TEXT NOT NULL,
  alias TEXT,
  nickname TEXT,
  typing TEXT,
  role TEXT,
  session TEXT,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_credentials(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  profile TEXT NOT NULL,
  present INTEGER,
  hint TEXT,
  label TEXT,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_cron_jobs(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  id TEXT NOT NULL,
  name TEXT,
  schedule TEXT,
  next_fire_unix INTEGER,
  paused INTEGER,
  last_run_unix INTEGER,
  last_ok INTEGER,
  last_detail TEXT,
  fire_count INTEGER,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_custom_providers(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  id TEXT NOT NULL,
  display_name TEXT,
  base_url TEXT,
  wire_selector TEXT,
  requires_key INTEGER,
  credential_ref TEXT,
  source TEXT,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_fleet_units(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  id TEXT NOT NULL,
  kind TEXT,
  state TEXT,
  work TEXT,
  profile TEXT,
  session TEXT,
  title TEXT,
  role TEXT,
  lifetime TEXT,
  child_count INTEGER,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_installed_models(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  id TEXT NOT NULL,
  display_name TEXT,
  local_path TEXT,
  size_bytes INTEGER,
  quant TEXT,
  installed_at_ms INTEGER,
  arch TEXT,
  context_length INTEGER,
  mmproj_path TEXT,
  sha256 TEXT,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_notifications(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  id TEXT NOT NULL,
  account TEXT,
  created_ms INTEGER,
  read INTEGER,
  title TEXT,
  subtitle TEXT,
  icon_name TEXT,
  interactive INTEGER,
  persistent INTEGER,
  kind TEXT,
  deleted INTEGER,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_persons(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  id TEXT NOT NULL,
  alias TEXT,
  avatar_blob TEXT,
  endpoint_count INTEGER,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_person_endpoints(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  person TEXT NOT NULL,
  transport TEXT NOT NULL,
  contact TEXT NOT NULL,
  display_name TEXT,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_profiles(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  id TEXT NOT NULL,
  provider TEXT,
  model TEXT,
  is_active INTEGER,
  owner TEXT,
  spec_blob BLOB,
  soul TEXT,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_provider_descriptors(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  id TEXT NOT NULL,
  display_name TEXT,
  kind TEXT,
  wire_selector TEXT,
  requires_key INTEGER,
  supports_model_discovery INTEGER,
  default_base_url TEXT,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_remembered_fingerprints(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  session TEXT NOT NULL,
  fingerprint TEXT NOT NULL,
  label TEXT,
  remembered_at_ms INTEGER,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_rooms(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  transport TEXT NOT NULL,
  room TEXT NOT NULL,
  name TEXT,
  session TEXT,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_route_pins(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  origin_key TEXT NOT NULL,
  transport TEXT,
  session TEXT,
  profile TEXT,
  isolation TEXT,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_saved_presences(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  id TEXT NOT NULL,
  name TEXT,
  primitive TEXT,
  message TEXT,
  emoji TEXT,
  last_used INTEGER,
  use_count INTEGER,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_sessions(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  session TEXT NOT NULL,
  state TEXT,
  title TEXT,
  bound_profile TEXT,
  last_activity_ms INTEGER,
  lifecycle TEXT,
  role TEXT,
  parent TEXT,
  pinned INTEGER,
  archived INTEGER,
  rewindable INTEGER,
  model TEXT,
  checkpoints INTEGER,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_tools(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  name TEXT NOT NULL,
  description TEXT,
  enabled INTEGER,
  requires_config TEXT,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

CREATE TABLE m_transport_accounts(
  key TEXT PRIMARY KEY,  -- canonical composite key (§3.1)
  transport TEXT NOT NULL,
  family TEXT,
  display_name TEXT,
  connection TEXT,
  presence TEXT,
  bound_profile TEXT,
  reason TEXT,
  message TEXT,
  fatal INTEGER,
  enabled INTEGER,
  label TEXT,
  settings_json TEXT,
  last_rev INTEGER NOT NULL, fetched_at_ms INTEGER NOT NULL);

-- W ChatMessage: per-scope window (policy 'chat', §4.6); item = canonical CBOR blob
CREATE TABLE w_chat_messages(
  scope TEXT NOT NULL,
  cursor INTEGER NOT NULL,
  payload BLOB NOT NULL,
  origin_op TEXT, last_rev INTEGER NOT NULL,
  PRIMARY KEY(scope, cursor));

-- W FsEntry: per-scope window (policy 'fs', §4.6); item = canonical CBOR blob
CREATE TABLE w_fs_entries(
  scope TEXT NOT NULL,
  path TEXT NOT NULL,
  payload BLOB NOT NULL,
  origin_op TEXT, last_rev INTEGER NOT NULL,
  PRIMARY KEY(scope, path));

-- W TranscriptBlock: per-scope window (policy 'transcript', §4.6); item = canonical CBOR blob
CREATE TABLE w_transcript_blocks(
  scope TEXT NOT NULL,
  seq INTEGER NOT NULL,
  payload BLOB NOT NULL,
  origin_op TEXT, last_rev INTEGER NOT NULL,
  PRIMARY KEY(scope, seq));

CREATE TABLE window_meta(
  kind TEXT NOT NULL, scope TEXT NOT NULL,
  item_count INTEGER, byte_count INTEGER,
  oldest_cursor INTEGER, newest_cursor INTEGER,
  contiguous_to_head INTEGER NOT NULL,
  PRIMARY KEY(kind, scope));

CREATE TABLE mirror_journal(rev INTEGER PRIMARY KEY, kind TEXT, key TEXT,
  op INTEGER, origin INTEGER, origin_op TEXT, at_ms INTEGER);

CREATE TABLE journal_watermarks(consumer TEXT PRIMARY KEY, rev INTEGER NOT NULL);

CREATE TABLE sync_cursors(name TEXT PRIMARY KEY, cursor INTEGER, epoch INTEGER);

CREATE TABLE node_revs(collection TEXT PRIMARY KEY, rev INTEGER,
  fetched_at_ms INTEGER, state INTEGER, last_error TEXT);

