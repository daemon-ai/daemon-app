# DaemonApp.ConversationsList

The list of chat threads for the current sidebar scope: a top bar (label / search /
new chat) and the scrollable list with its delegates. Backed by a list model over
`core` data.
